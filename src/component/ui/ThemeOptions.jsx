import { createTheme } from "@mui/material";

export const themeOption = createTheme({
    palette: {
        primary: {
            main: '#99173c',
        },
        secondary: {
            main: '#2d4180',
        },
    },
});